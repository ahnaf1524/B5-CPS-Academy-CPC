#include<iostream>
using namespace std;

const int maxN = 105;
string s[maxN];

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int tc; 
  cin >> tc;

  while(tc--) { 
    int n; cin >> n;
    string str; cin >> str;
    cout << str.size() << endl;
  }

  return 0;
}
